import { StyleSheet, Text, View } from 'react-native';

function GuestMenuScreen() {
  return (
    <View>
      <Text>GuestMenuScreen</Text>
    </View>
  );
}

export default GuestMenuScreen;

const styles = StyleSheet.create({});
